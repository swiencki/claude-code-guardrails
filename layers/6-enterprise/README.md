# Enterprise Policies

Organization-managed policy templates that override user and project settings.

This layer is planned. Enterprise policies are configured via managed-settings.json by administrators, not via this build system.
