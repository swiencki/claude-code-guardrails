{
  "description": "Blocks destructive kubectl operations: delete, drain, exec.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '.tool_input.command // \"\" | test(\"kubectl\\\\s+(delete|drain|exec)\") | not' >/dev/null",
            "statusMessage": "Checking for destructive kubectl commands"
          }
        ]
      }
    ]
  }
}
