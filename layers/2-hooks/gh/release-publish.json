{
  "description": "Blocks gh release create to prevent unintended releases.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | jq -e '.command | test(\"gh\\\\s+release\\\\s+create\") | not'",
            "statusMessage": "Checking for gh release create"
          }
        ]
      }
    ]
  }
}
